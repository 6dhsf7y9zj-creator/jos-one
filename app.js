const $=s=>document.querySelector(s);const money=n=>new Intl.NumberFormat('en-GB',{style:'currency',currency:'GBP'}).format(Number(n||0));
const defaultItems=(window.JOS_SEED_ITEMS||[]);let state={page:'home',items:JSON.parse(localStorage.getItem('jos-items')||'null')||defaultItems,orders:JSON.parse(localStorage.getItem('jos-orders')||'null')||[{id:'ORD-1001',sku:'JAE-PRE-006',item:'Nike Blue/Gold Hoodie',status:'Ready to pack',deadline:'Tomorrow'},{id:'ORD-1002',sku:'JAE-PRE-004',item:'Tommy Womens T-Shirt',status:'Ready to dispatch',deadline:'Today'}],filter:'All',query:''};
function save(){localStorage.setItem('jos-items',JSON.stringify(state.items));localStorage.setItem('jos-orders',JSON.stringify(state.orders))}
function metrics(){const live=state.items.filter(x=>['Live','Photographed','Prep'].includes(x.status)).length;const cost=state.items.reduce((s,x)=>s+(x.landedCost||x.purchasePrice||0),0);const profit=state.items.reduce((s,x)=>s+(x.expectedProfit||0),0);const waiting=state.items.filter(x=>x.status==='Prep').length;return{live,cost,profit,waiting}}
function header(sub){return `<div class="top"><div class="brand"><img class="logo" src="assets/the-jae-edit-logo.png"><div><div class="eyebrow">JOS One · Alpha 0.1</div><div class="title">${sub}</div><div class="sub">The Operating System for The JAE Edit</div></div></div></div>`}
function nav(){return `<div class="bottom"><div class="nav">${[['home','⌂','Home'],['inventory','▦','Inventory'],['add','＋','Add'],['source','◉','SourceCheck'],['orders','▣','Orders']].map(([p,i,l])=>`<button data-page="${p}" class="${state.page===p?'active':''}"><strong>${i}</strong>${l}</button>`).join('')}</div></div>`}
function home(){const m=metrics();const health=Math.max(55,Math.min(98,92-Math.min(20,m.waiting)));return `${header('CEO Dashboard')}<main class="content"><div class="card health"><div><div class="eyebrow">Business Health</div><div class="score">${health}/100</div><span class="pill">● Strong</span></div><img class="logo" style="width:78px;height:78px" src="assets/the-jae-edit-logo.png"></div><div class="card mission"><div class="eyebrow">Tonight's Mission</div><h2>${m.waiting?`Prepare ${Math.min(m.waiting,8)} stock items`:'Review launch inventory'}</h2><p>Highest-impact task based on your current workflow.</p><button class="btn full" data-page="inventory">Start Mission</button></div><h2 class="section-title">CEO Snapshot</h2><div class="grid"><div class="metric"><span>Inventory Cost</span><b>${money(m.cost)}</b></div><div class="metric"><span>Active Stock</span><b>${m.live}</b></div><div class="metric"><span>Expected Profit</span><b>${money(m.profit)}</b></div><div class="metric"><span>Orders Ready</span><b>${state.orders.length}</b></div></div><h2 class="section-title">Business Alerts</h2><div class="card"><div class="alert"><span>🟡</span><div><b>${m.waiting} items need preparation</b><div class="meta">Complete these before adding more backlog.</div></div></div><div class="alert"><span>🟠</span><div><b>${state.items.filter(x=>x.daysInStock>60).length} items over 60 days</b><div class="meta">Review pricing and launch suitability.</div></div></div><div class="alert"><span>🟢</span><div><b>No dispatch overdue</b><div class="meta">Next-day dispatch target is protected.</div></div></div></div></main>${nav()}`}
function inventory(){let items=state.items.filter(x=>{const q=state.query.toLowerCase();const hit=!q||[x.sku,x.brand,x.description,x.storage].join(' ').toLowerCase().includes(q);const f=state.filter==='All'||x.status===state.filter||x.grade===state.filter;return hit&&f});return `${header('Inventory Command Centre')}<main class="content"><input id="search" class="search" placeholder="Search SKU, brand, item or location" value="${state.query}"><div class="filters">${['All','Prep','Photographed','Live','A','B','C'].map(f=>`<button class="chip ${state.filter===f?'active':''}" data-filter="${f}">${f}</button>`).join('')}</div><div class="grid"><div class="metric"><span>Total Stock</span><b>${state.items.length}</b></div><div class="metric"><span>Prep Queue</span><b>${state.items.filter(x=>x.status==='Prep').length}</b></div><div class="metric"><span>Photographed</span><b>${state.items.filter(x=>x.status==='Photographed').length}</b></div><div class="metric"><span>Exit Review</span><b>${state.items.filter(x=>x.daysInStock>90).length}</b></div></div><h2 class="section-title">Items</h2>${items.length?items.map((x,i)=>`<div class="card item" data-item="${state.items.indexOf(x)}"><div><h3>${x.brand} · ${x.category}</h3><div class="meta"><b>${x.sku}</b><br>${x.description}<br>${x.size} · ${x.condition} · ${x.status}</div><div style="margin-top:9px"><span class="grade">Grade ${x.grade||'—'}</span> <span class="grade">${x.storage}</span></div></div><div class="money"><b>${money(x.expectedSale||x.listPrice)}</b><div class="meta">Profit ${money(x.expectedProfit)}</div></div></div>`).join(''):'<div class="empty">No matching stock.</div>'}</main>${nav()}`}
function add(){return `${header('Add New Stock')}<main class="content"><div class="card"><div class="eyebrow">Fast Entry</div><h2 style="margin:6px 0 16px">Add an item in under 60 seconds</h2><form id="addForm" class="form-grid two"><div class="field"><label>Brand *</label><input name="brand" required></div><div class="field"><label>Category *</label><select name="category"><option>Hoodie</option><option>T-Shirt</option><option>Jacket</option><option>Jeans</option><option>Jumper</option><option>Fleece</option><option>Other</option></select></div><div class="field"><label>Size</label><input name="size"></div><div class="field"><label>Condition *</label><select name="condition"><option>Excellent</option><option>Very good</option><option>Good</option><option>Needs attention</option></select></div><div class="field"><label>Purchase price *</label><input name="purchase" type="number" step="0.01" required></div><div class="field"><label>Expected sale</label><input name="sale" type="number" step="0.01"></div><div class="field"><label>Source</label><select name="source"><option>Charity Shop</option><option>Vinted</option><option>Car Boot Sale</option><option>Bundle</option><option>Other</option></select></div><div class="field"><label>Storage</label><input name="storage" placeholder="e.g. Box A1"></div><button class="btn full" style="grid-column:1/-1">Save to Inventory</button></form></div></main>${nav()}`}
function source(){return `${header('SourceCheck')}<main class="content"><div class="card"><div class="eyebrow">Decision Engine</div><h2 style="margin:6px 0 16px">Should The JAE Edit buy it?</h2><form id="sourceForm" class="form-grid two"><div class="field"><label>Brand</label><input name="brand" required></div><div class="field"><label>Item type</label><select name="category"><option>Hoodie</option><option>Jacket</option><option>Jeans</option><option>T-Shirt</option><option>Jumper</option><option>Fleece</option></select></div><div class="field"><label>Condition</label><select name="condition"><option>Excellent</option><option>Very good</option><option>Good</option><option>Needs attention</option></select></div><div class="field"><label>Buy price</label><input name="buy" type="number" step="0.01" required></div><div class="field"><label>Expected sale price</label><input name="sale" type="number" step="0.01" required></div><div class="field"><label>Authenticity confidence</label><select name="auth"><option>High</option><option>Medium</option><option>Low</option></select></div><button class="btn full" style="grid-column:1/-1">Analyse Item</button></form></div><div id="sourceResult"></div></main>${nav()}`}
function orders(){return `${header('Orders & Dispatch')}<main class="content"><div class="grid"><div class="metric"><span>Ready to Pack</span><b>${state.orders.filter(x=>x.status.includes('pack')).length}</b></div><div class="metric"><span>Ready to Dispatch</span><b>${state.orders.filter(x=>x.status.includes('dispatch')).length}</b></div></div><h2 class="section-title">Order Queue</h2>${state.orders.map((o,i)=>`<div class="card"><div class="item"><div><h3>${o.item}</h3><div class="meta">${o.id} · ${o.sku}<br>Deadline: ${o.deadline}</div><div style="margin-top:9px"><span class="grade">${o.status}</span></div></div><button class="btn secondary" data-dispatch="${i}">Complete</button></div></div>`).join('')||'<div class="empty">No active orders.</div>'}</main>${nav()}`}
function itemModal(i){const x=state.items[i];return `<div class="modal" id="modal"><div class="sheet"><button class="close" data-close>×</button><div class="eyebrow">${x.sku}</div><h2 style="font-family:Georgia,serif;font-size:27px;margin:8px 0">${x.brand} ${x.category}</h2><p class="meta">${x.description}</p><div class="grid" style="margin-top:16px"><div class="metric"><span>Landed Cost</span><b>${money(x.landedCost||x.purchasePrice)}</b></div><div class="metric"><span>Expected Sale</span><b>${money(x.expectedSale||x.listPrice)}</b></div><div class="metric"><span>Expected Profit</span><b>${money(x.expectedProfit)}</b></div><div class="metric"><span>Days in Stock</span><b>${x.daysInStock}</b></div></div><div class="card" style="margin-top:14px"><b>Status</b><div class="meta">${x.status} · Grade ${x.grade} · ${x.storage}</div><hr style="border:0;border-top:1px solid var(--line);margin:14px 0"><b>Recommended action</b><div class="meta">${x.action||'Review item'}</div></div></div></div>`}
function render(){let html=state.page==='home'?home():state.page==='inventory'?inventory():state.page==='add'?add():state.page==='source'?source():orders();$('#app').innerHTML=`<div class="app">${html}</div>`;bind()}
function bind(){document.querySelectorAll('[data-page]').forEach(b=>b.onclick=()=>{state.page=b.dataset.page;render()});document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{state.filter=b.dataset.filter;render()});const s=$('#search');if(s)s.oninput=e=>{state.query=e.target.value;render()};document.querySelectorAll('[data-item]').forEach(el=>el.onclick=()=>document.body.insertAdjacentHTML('beforeend',itemModal(Number(el.dataset.item))));document.body.onclick=e=>{if(e.target.matches('[data-close]')||e.target.id==='modal')$('#modal')?.remove()};const af=$('#addForm');if(af)af.onsubmit=e=>{e.preventDefault();const f=new FormData(af);const n=state.items.length+1;const buy=Number(f.get('purchase'));const sale=Number(f.get('sale')||buy*3);state.items.unshift({sku:`JAE-${String(n).padStart(4,'0')}`,brand:f.get('brand'),category:f.get('category'),description:`${f.get('brand')} ${f.get('category')}`,size:f.get('size'),condition:f.get('condition'),status:'Prep',grade:'B',storage:f.get('storage')||'TBC',purchasePrice:buy,landedCost:buy,expectedSale:sale,listPrice:sale,expectedProfit:sale-buy,roi:buy?((sale-buy)/buy):0,daysInStock:0,action:'Prepare item'});save();state.page='inventory';render()};const sf=$('#sourceForm');if(sf)sf.onsubmit=e=>{e.preventDefault();const f=new FormData(sf),buy=Number(f.get('buy')),sale=Number(f.get('sale')),cond=f.get('condition'),auth=f.get('auth');const profit=sale-buy-1.5,roi=buy?profit/buy:0;let score=50+Math.min(20,profit)+Math.min(15,roi*4)+(cond==='Excellent'?10:cond==='Very good'?7:cond==='Good'?2:-20)+(auth==='High'?5:auth==='Medium'?1:-20);score=Math.max(0,Math.min(100,Math.round(score)));let d=score>=85?'BUY':score>=70?'BUY SELECTIVELY':score>=55?'CAUTION':'PASS';if(cond==='Needs attention'||auth==='Low'||profit<8)d='PASS';$('#sourceResult').innerHTML=`<div class="card result ${d==='BUY'?'buy':d.includes('SELECTIVELY')||d==='CAUTION'?'caution':'pass'}"><div class="eyebrow">Recommendation</div><div class="decision">${d}</div><div class="score">${score}/100</div><p class="meta">Expected profit ${money(profit)} · ROI ${Math.round(roi*100)}%<br>Conservative sale ${money(sale*.82)} · Maximum recommended buy ${money(Math.max(0,sale-1.5-15))}</p></div>`};document.querySelectorAll('[data-dispatch]').forEach(b=>b.onclick=()=>{state.orders.splice(Number(b.dataset.dispatch),1);save();render()})}
render();
