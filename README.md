# JOS One Engineering 1.0.2 — Flat Recovery

This recovery build keeps every required web-app file at the repository root. It avoids folder-upload failures on iPhone Safari and includes the working stock editing hotfix.
